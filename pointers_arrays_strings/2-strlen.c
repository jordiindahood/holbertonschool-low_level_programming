#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - check the code
 * Description: return a lenght of a string
 * @s : pointer to string
 * Return: int
 */
int _strlen(char *s)
{
	return(strlen(*s));
}
