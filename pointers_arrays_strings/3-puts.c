#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _puts - check the code
 * Description: print a string, followed by a new line
 * @str : pointer to string
 * Return: void
 */
void _puts(char *str)
{
	puts(str);
	_putchar('\n');
}
