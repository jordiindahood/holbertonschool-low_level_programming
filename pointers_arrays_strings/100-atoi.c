#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
/**
 * _atoi - check the code
 * Description: convert a string to integer
 * @s : pointer to string
 * Return: vaule converted from string
 */
int _atoi(char *s)
{
	
	return (atoi(s));
}
